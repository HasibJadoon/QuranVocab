const DOMAIN = '';

export const environment = {
  production: false,
  useHash: false,
  cordova: false,
  apiUrl: DOMAIN + '/api',
  useCookie: false,
  analytics: false,
  support: false,
  watchLog: false
};
