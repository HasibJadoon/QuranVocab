/**
 * Polyfills loaded before the app.
 *
 * Angular 20 does not require extra polyfills by default, but the spec
 * tsconfig includes this file, so we keep it as an explicit entry point.
 */
import 'zone.js';
